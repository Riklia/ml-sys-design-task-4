[project]
name = "ml-sys-design-task-4"
version = "0.1.0"
dependencies = []

[tool.setuptools]
py-modules = ["katib_manager"]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
